 # ✅ Python Readiness Quiz | اختبار جاهزية بايثون
 
 **Purpose:** Check if you're ready for AI notebooks.
 
 **Target Score:** 8/10  
 **If below 8:** Review `DOCS/BRIDGE_PYTHON.md`
 
 ---
 
 ## Questions
 
 1. Which data structure is best for key-value pairs?
    - A) List
    - B) Tuple
    - C) Dictionary
    - D) Set
 
 2. What does this return?
    ```python
    [x * 2 for x in [1, 2, 3]]
    ```
    - A) [1, 2, 3]
    - B) [2, 4, 6]
    - C) [1, 4, 9]
    - D) Error
 
 3. Which line correctly defines a function?
    - A) `function add(a, b): return a + b`
    - B) `def add(a, b): return a + b`
    - C) `add(a, b) => a + b`
    - D) `func add(a, b) = a + b`
 
 4. What does `df.head()` do in pandas?
    - A) Shows last 5 rows
    - B) Shows first 5 rows
    - C) Returns column names only
    - D) Deletes empty rows
 
 5. Which library is commonly used for numerical arrays?
    - A) matplotlib
    - B) pandas
    - C) numpy
    - D) seaborn
 
 6. What does `len([1, 2, 3])` return?
    - A) 2
    - B) 3
    - C) 4
    - D) Error
 
 7. Which expression filters rows where `age > 30`?
    - A) `df.filter(age > 30)`
    - B) `df[df.age > 30]`
    - C) `df.where(age > 30)`
    - D) `df.select(age > 30)`
 
 8. What does this print?
    ```python
    x = 5
    if x > 3:
        print("Yes")
    ```
    - A) No output
    - B) Yes
    - C) True
    - D) Error
 
 9. Which line reads a CSV file into pandas?
    - A) `pd.load_csv("file.csv")`
    - B) `pd.read_csv("file.csv")`
    - C) `pd.open("file.csv")`
    - D) `pd.csv("file.csv")`
 
 10. What does `range(3)` produce?
     - A) [1, 2, 3]
     - B) [0, 1, 2]
     - C) [0, 1, 2, 3]
     - D) Error
 
 ---
 
 ## Answer Key
 
 1) C  
 2) B  
 3) B  
 4) B  
 5) C  
 6) B  
 7) B  
 8) B  
 9) B  
 10) B  
 
 ---
 
 **Next Step:** If your score is below 8/10, review `DOCS/BRIDGE_PYTHON.md`.
